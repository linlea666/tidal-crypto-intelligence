# Product Design 验收

final result: passed

日期：2026-09-24。源图：`docs/design/selected.png`。实现：本地浏览器 `http://localhost:5173/?demo=1#liquidity`；桌面证据 `docs/qa/desktop.png`，手机证据 `docs/qa/mobile.png`。演示夹具仅用于匹配源图，生产构建只使用真实 API。

## 比较条件与证据

- 源图 1487×1058 像素；桌面 CSS 视口 1487×1058，原生浏览器截图 1472×1047，来自浏览器对内容区域的轻微缩放和滚动条裁边，未拉伸图片。手机 CSS 390×844，原生截图 375×812。
- 同一暗色状态、BTC、100 美元价档、±2%、至少 5 分钟、选中 83,800–83,899。源图与更新后的桌面截图放在同一次图像比较输入中检查。
- 全图检查了导航、标题、左侧四买四卖、当前价分隔、右侧金额/证据/来源、底部历史。重点逐项比较价格字体、所选行、柱长与来源条；原图和截图中的文字均可读，无需再次裁切放大。
- 移动端实际 DOM：innerWidth=390，document.scrollWidth=375，无页面横向溢出。主导航可水平滚动，价位金额可见，证据面板在下方。

## 发现与修正记录

1. [P1，已修复] 浏览器原生 meter 内部轨道导致金额柱显示过细。明确 meter 内部轨道高度和填色；修复后截图中所有买卖柱约 23px，同一金额共用比例尺。
2. [P2，已修复] 初版过大的垂直间距使历史区离开首屏。压缩行距与面板留白，保留源图约 7:3 主次布局；最终桌面截图中历史图和底部说明可见。
3. [P2，已修复] 手机导航滚动条干扰内容。保留滚动能力并隐藏导航滚动条；移动证据确认未遮挡主内容。
4. [P2，已修复] ECharts 默认坐标文字在暗色背景对比不足。补充统一暗色轴和图例主题；最终截图显示清晰的时间与金额刻度。
5. [P2，已修复] 手工标注弹窗缺少键盘退出与焦点管理。加入初始焦点、Tab 边界、Escape 关闭、关闭后恢复触发按钮焦点；浏览器已验证。

## 五项保真检查

- 字体：Inter 数字和界面拉丁文字、PingFang SC 中文回退；标题/现价/所选金额层级清晰，价格使用等宽数字。源图为生成图，细微字形、抗锯齿和字重差异归为 P3。
- 间距：约 24px 页边距，横向价位行、细分隔线、右侧证据面板和紧凑历史符合选定方向；真实六页导航及周期/手工标注控制是方案要求的扩展。
- 色彩：石墨背景、薄荷绿买单和珊瑚红卖单，金额与证据含义分开。OI 图例和曲线对应颜色已统一；坐标对比度已修复。
- 图像：源图无必须单独提取的插画或照片。TIDAL 为文字标识，标准图标使用 Phosphor，柱子和数据图采用可访问的原生控件/图表，不使用整图覆盖。
- 文案：保留直观柱长说明；“部分撤走”改成“疑似撤走”，强度改为同类比较；空清算价、持仓均价、已监控地址范围和成交净买卖口径明确。源图历史侧边样本表改为交互图与回看筛选，服务真实数据查询。

## 已验证交互

真实数据登录、BTC/ETH 切换、价档选择、点击买卖行更新右侧明细、历史热力图切换、成交周期切换、合约 OI/资金费率、巨鲸前10/50和空清算价、保留策略保存、手工标注保存与盈亏比、弹窗 Escape 和焦点恢复。本地测试标注已通过 API 清除。控制台最新错误检查为空。

## 后续与边界

2026-09-24 追加功能验收：默认选中价位改为当前真实数据，移除生产态的演示价位默认值；BTC/ETH 切换会重置选择，展开后选择较远价位再收起，该行仍在重点列表内。实际浏览器验证通过，控制台无错误；暗色样式、演示对照状态及布局不变。

- [P3] 源图柱状区细竖网格线和字形的进一步匹配可后续微调，不影响读数。
- 本报告仅表示界面验收通过。真实服务器发布、自动回滚、证书续期与 72 小时运行验收见 `docs/STATUS.md`，不得用视觉验收代替。
- 检查清单：源图比较完成；P0/P1/P2 已处理；桌面和手机截图已保存；主要流程已验证。
