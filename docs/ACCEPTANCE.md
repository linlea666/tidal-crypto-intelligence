# 发布与运行验收

## V2.4 发布前检查：进行中

本地新增财务参考对账、267条双列完整分页、单侧筛选隔离、相同快照共同尺度、缺失/过期FX、明确结束及未返回状态、原币减量、修订撤回、迟到足迹/跨来源隔离、历史容量和在线备份测试通过。100次本地读取没有上游调用。Go全量race/vet及前端类型/构建/测试已通过；隔离部署控制流验证启动失败及健康失败都恢复上一镜像/配置。

Linux前后剖析、包含完整维护/30天盘口及90天研究的短回放、至少5小时15分钟同规格长回放尚待完成，不能发布或宣称资源达标。浏览器已验证1440px双列、390px切换保留页码、共同金额尺度及详情Escape焦点返回；200%等效CSS视口已检查无横向溢出，其他回归继续。新版未部署、未创建验收起点，V2.3失败证据不变。

## V2.3.1 本轮结论：资源目标未通过，提前结束

2026-09-25读取完整受限报告，固定起点仍为 **2026-09-25T02:13:38.071953Z**，以最后服务器采样 **2026-09-25T07:19:44.349425Z** 作为结束，共5.102小时。没有重置起点、跨版本拼接或静默延长；本次不发布生产版本。自动验收已停用，应用和服务器五分钟采样保持运行。原起点与V2.2.1失败证据哈希均未改变；新结束、完整报告和后续HTTPS核对结果保存在本地忽略目录。

| 检查项 | 本轮证据及结论 |
| --- | --- |
| 连续性、固定身份 | 62个样本，首样本等于固定起点，无重复采样；最大间隔302.133秒。version/revision/digest及应用、网关镜像均固定，健康/就绪失败、重启和OOM均为0 |
| 内存：未通过 | Docker MemUsage口径38/62个样本超过600MiB，全段中位621.35MiB、峰值678.8MiB、末值658.3MiB；最近4小时38/48个超标、中位627.2MiB。容器上限仍768MiB；持续不达目标即提前结束，不称为72小时通过 |
| CPU及诊断限制 | 单核口径0.22–143.69%，中位126.785%，容器1.7核。07:25:33 HTTPS运行数据为Go活动堆158,955,472字节、HeapSys456,065,024字节、22个goroutine；受限报告没有独立进程RSS/分配剖析，不能将容器与Go堆差值直接认定泄漏、SQLite或缓存根因 |
| 核心数据 | 采样中generation=v2、legacyCollectorsRunning=false；各BTC/ETH五家盘口、价格均有效。默认现货/合约成交、普通OI、资金费率、足迹、24小时清算模型、大单及基础巨鲸无过期记录。最后基础巨鲸BTC342/342、ETH242/242条有效 |
| 来源时间限制 | 健康文件的状态以collector.at为准，外层utc稍后才采集容器；未把该缓存当实时市场时间。最新HTTPS另行核验。FX及大单、钱包列表仍为retrieval_only，没有伪造源时间 |
| 辅助时效 | BTC溢价19个采样过期，BTC专用OI历史5个采样过期，末次恢复有效；辅助权重仍0。ETH专用OI停用及两个7天按需清算缓存过期是计划/已完成按需状态，不误报当前核心断流 |
| 配额、队列 | 起点后3308次请求，按采样跨度平均约10.81次/分钟（含补采/重试），新增429及认证错误为0；离散采样中滚动窗口最大12次、并发最大1个。单次采样不能证明所有瞬间合规，限流另有自动测试；当前任务无连续失败，signalError/studyError/researchGap均为空 |
| 存储 | 项目1,042,357,409→1,113,332,151字节，增长约67.7MiB；末次研究约43.2MiB、大单历史31.3MiB、巨鲸6.7MiB、磁盘空闲31.7GiB，容量暂停均false，SQLite3.51.3 |
| 本地API与分页 | HTTPS业务接口约986–1552ms（含网络/TLS），服务器健康查询4.18–55ms；无API错误。固定快照BTC349、ETH323条全分页、无重复、排序与跨页共同尺度稳定；ETH信号关闭，普通数据继续 |
| 研究进度 | 新BTC研究queueCursor=24，最终按覆盖标incomplete，非卡住不推进。三个案例各120/120小时成交、价格、溢价；成交已有完整五分钟事实可汇总，8月19日OI119/120。90天成交覆盖约38.9%，严格策略未完成；当前BTC基线29个有效日期、100%覆盖，当前窗口完整 |
| 其他独立状态 | 4小时activity完整；24小时约88.4%且缺边界，正确降级。钱包小时/6小时、ETF每日规则保持，ETF最后已报告日09-23；14天前向与SMTP实发仍未通过，SMTP未配置且未发送测试邮件。证书至2026-09-30T21:47:22Z有效 |

本轮只使用专用monitor_key受限报告和认证看板HTTPS读取，没有旁路请求CoinGlass。后续页面设计与风险标签仍待用户确认，生产代码未改；资源修复须先定位并在同规格、同数据量及足够运行时间下验证，短时累计分配优化不代替常驻内存验收。

## V2.3.1 发布验证与独立连续验收

最终版本 [v2.3.1](https://github.com/linlea666/tidal-crypto-intelligence/releases/tag/v2.3.1) 已完成本地→GitHub检查→正式Release→不可变镜像→生产。revision `6ebf0839d49167aa5995702aafe9feeed31a61be`，digest `sha256:5724cfae8a763b41e9cab27affdec599fca0c0bf317066d45430490116130db5`。v2.3.0发布前生命周期测试在每小时末十分钟跨入下一基线小时，注入的简化测试基线正确失效，因测试假设不成立而阻断发布；未进入生产。v2.3.1仅固定该测试的时间区间，生产规则不放宽。

自动测试覆盖137条固定快照分页/同值排序/统一比例尺、失效FX/未知时间、小时与五分钟隔离、分范围错误与归档恢复、BTC范围/旧ETH通知抑制、100次本地GET零上游、滚动基线与原算法逐点一致及历史修订失效。Go race/vet、前端类型/构建/基础测试、在线SQLite备份恢复及启动失败/健康失败隔离回退通过。桌面1440px、手机390px检查列表50/100条、跨页、筛选、详情、三块解释、案例图表及已有页面，无横向溢出或浏览器错误；上线后认证HTTPS接口、TLS与就绪检查通过。

[最终补丁Linux资源回放](https://github.com/linlea666/tidal-crypto-intelligence/actions/runs/36084615394)：1.7核、768MiB容器、Go软限512MiB，90天补采/完整研究/并发本地读，270.58秒、60样本，峰值44.96MiB、中位39.345MiB、末值40.58MiB，峰值CPU104.43%（单核口径），退出0、OOM false。该短测是发布门槛，不能当生产72小时通过，也不能将累计分配量当常驻内存。

生产分页核对BTC 347条、ETH 322条（当时有效345/320条），50条首屏与100条分页一致，全部身份去重、排序与固定比例尺正确。大资金接口原大单兼容字段为空并指向独立页，三块结构化解释保留同窗买卖及价格。ETH新研究/信号禁用，普通现货、成交、合约、清算、巨鲸、钱包和ETF接口正常。

三个指定案例在生产均为 `hourly_available`：各120小时成交/价格/溢价；8月19日OI 119小时、其余120小时。实际成交粒度3600秒，没有计入五分钟覆盖。队列24个范围任务，已有小时事实本地复用；严格策略 `incomplete`，仍按范围披露分钟缺口。信号/研究错误及researchGap为空，辅助权重0，SMTP未配置不补发旧邮件。

V2.3固定起点 **2026-09-25T02:13:38.071953Z**，满72小时 **2026-09-28T02:13:38.071953Z**。采用部署预热后首个健康、数据有效且低于600MiB的服务器采样，不含02:12启动样本。原点/初始完整报告/后续报告位于本地忽略的 `secrets/soak-v2.3-*`；V2.2.1失败证据的SHA-256核对未变。受限历史报告不包含researchAssets字段时，以同样本BTC信号质量、ETH专用OI停用状态及HTTPS启用币种接口共同核实，未伪造采样字段。

起点应用448.1MiB、CPU127.55%，网关4.09MiB，容器healthy/0 restart/OOM false；BTC/ETH各5/5盘口，价格/FX有效。项目1,042,357,409字节、研究37,302,736字节、大单历史23,333,152字节、巨鲸5,193,728字节、磁盘空闲34,125,176,832字节；容量暂停均false，SQLite3.51.3。额度calls=9275/rateLimited=1，滚动窗口10次、无认证错误；累计429为旧事件，不计为新版新增。ETH专用OI主动停用、两个已完成7天按需清算缓存过期均不作为实时断流。证书至2026-09-30T21:47:22Z有效。

自动验收恢复为每小时检查、状态正常无变化保持安静。72小时运行、14天前向观察、90天完整策略、SMTP实发仍分别未完成；不降低数据质量阈值。后续普通文档提交不改变生产Release与固定验收身份。详情见 [V2.3数据与页面说明](V2.3-ORDERS-STUDIES.md)。


2026-09-24。界面证据与交互检查见 [Product Design 验收](../design-qa.md)，当前进度见 [STATUS](STATUS.md)。

## V2.2 历史部署验证与连续运行（资源未通过）

[v2.2.1](https://github.com/linlea666/tidal-crypto-intelligence/releases/tag/v2.2.1) 已完成正式发布及健康检查；revision `89461c165b5be038d60f3d06795e021a8460caf5`，digest `sha256:e54614c8d264e14c388462bdc2af81e50aece0ba19b909431cb7677177d834c9`。主功能见PR #3；补丁仅将缺失钱包总额返回null并扩展对应类型/边界测试。当前运行版本不随后续普通文档提交改变。

回归覆盖共同来源/别名冲突/缺失导致方向反转/非24小时间隔、ETF未收盘/未确认零值/节假日/基金对账、事实首次可用时间与修订、买卖对称/基线95%/冻结阈值/状态通知原子提交、重启不补发、容量满时实时继续、并发研究去重和GET零上游。Go race/vet、前端类型/构建/基础测试、新库在线备份恢复均通过。隔离启动及健康失败回退通过，未向新版生产注入故障。

真实9个新增数据集全部通过契约；两个资产钱包各15家列表来源，原始来源时间缺失仍为retrieval_only。BTC历史90个相邻变化、ETH97个相邻变化可从本地90天范围读取，间隔及共同覆盖独立展示，不伪装逐日固定采样。ETF当前读取88个时间点：BTC59个已报告、2个分项缺失、1个未结束、26个非交易日；ETH59个已报告、1个分项缺失、1个零值待确认、1个未结束、26个非交易日。被计入的基金总额独立十进制复算全部一致；异常/缺失记录未补零。该数量是本次观察值，后续会随保留窗口变化。

桌面1440px、手机390px已检钱包/ETF真实数字、BTC/ETH切换、逐日基金贡献、研究队列与数据不足停发。DOM宽度无溢出，浏览器错误为空。公开HTTPS查询为本地缓存读取，约1秒级包含网络和TLS，不能等同服务内耗时。程序测试不代表策略有预测价值。

V2.1保留原起点，在2026-09-24T17:38:44.586135573Z切换中止：3.801小时/46样本，健康失败0、峰值635.8MiB、最大间隔约5.15分钟。未满72小时且峰值超过600MiB稳态目标，不能记为通过。

V2.2正式固定起点 **2026-09-24T17:51:43.474629Z**，原计划完整72小时 **2026-09-27T17:51:43.474629Z**。V2.2.0预热与V2.2.1正式切换前样本分开存档，不计入新固定版本区间。原点、初始完整报告与最新报告保存在已忽略的`secrets/soak-v2.2-*`。本轮因持续超过内存目标提前结束，原起点及identity不变；不跨版本拼接或静默延长。

当前尚无SMTP配置，邮件实发不认定通过。90天研究与14天实时旁路观察尚未完成；基线不足会停发，研究仅依赖未知历史发布时间的部分只能给关联结论。钱包/溢价等指标仍权重0。详见 [V2.2数据与规则](V2.2-SIGNALS-WALLET-ETF.md)。

初始研究实取：BTC现货/合约从2026-06-26开始的第一批5分钟成交数据未返回有效记录；保留任务错误，不将空响应写为零，不据此声称全部90天不可用。后续7个历史任务均在5次失败后停用：4个成交历史返回无可用数据，2个OI历史及1个溢价历史返回业务码400。BTC/ETH价格历史游标已到终点，研究状态均为 `incomplete`、`coreCalculated=false`，仅保留关联研究模式；最新9个数据集仍verified。分钟历史不足与运行资源验收分开记录。

### V2.2 本轮结论：资源目标未通过，提前结束

实际读取截至 **2026-09-25T00:03:01.333900Z** 的完整服务器采样，与固定原点相隔 **6.188小时**。最后报告获取于00:03:20 UTC；本次审计使用实际报告时间，未用较早的心跳触发时间充当采样终点。结论已保存到已忽略的 `secrets/soak-v2.2-end.json`，完整报告为 `soak-v2.2-final-report.json`，同时保存原起点及报告SHA-256。停止本轮自动验收，生产应用与服务器五分钟采样继续运行；本次仅提交验收文档，不发布生产版本。

| 检查项 | 完整样本结果 |
| --- | --- |
| 连续性与版本 | 75个样本，首样本等于固定起点；最大相邻间隔301.97秒。version/revision/digest一致，应用与网关镜像未改变；原点容器健康探测为starting，之后healthy，无unhealthy、就绪失败、重启或OOM |
| 内存：未通过 | Docker MemUsage口径46/75个样本超过600MiB；最近4小时33/48个超过目标，中位数631.85MiB；全段峰值711.1MiB、末值648.9MiB，容器上限768MiB。属于持续未达资源目标，未运行满72小时，提前结束 |
| CPU与诊断边界 | 单核百分比峰值168.5%，多次在120–135%附近，也有低占用阶段；2核机器、应用CPU限额1.7核。00:02 HTTPS运行状态中活动Go堆约20MiB、heapSys约447MiB；不等同容器常驻内存，分配根因未由本次只读证据确定 |
| 当前市场数据 | generation=v2、legacyCollectorsRunning=false始终成立；BTC/ETH各5家盘口及价格有效、FX保持retrieval_only且未过期。默认成交、OI快照、资金费率、足迹、24小时清算模型、大单和基础巨鲸有效；末样本巨鲸BTC330/330、ETH243/243条符合逐条时效 |
| 辅助时效异常 | ETH聚合OI历史10个采样过期，已入库有效时点多次约10分钟才前进；12分钟有效期按源时间正确降级。BTC溢价2个采样过期。不能仅凭采集任务成功认定源数据新鲜；采集相位与上游有效时点延迟仍需核查 |
| 按需与大单历史 | 两个7天清算任务已成功完成，缓存过期不视为默认实时断流。已知state筛选差异仍保留原始已结束事实及历史不完整，没有将其补造为撤单 |
| 额度 | 起点后4092次请求，含补采/重试平均约11.02次/分钟；新增429=0、认证失败=0。五分钟采样所见滚动窗口最大12次、并发最大1个；离散采样不能单独证明每个瞬间均合规，限流完整性另有已有自动测试 |
| 存储 | 项目812,725,938→935,169,304字节，增长约116.8MiB；末值约891.8MiB。研究33.9MiB、大单历史18.7MiB、巨鲸4.2MiB；空闲31.9GiB，所有历史容量暂停标志为false，SQLite3.51.3 |
| 查询与证书 | 服务器健康查询4.39–56.16ms；最后HTTPS业务查询1.05–1.60秒，另查1/4/24小时活动窗口最高2.52秒，包含网络/TLS。无API错误；证书至2026-09-30T21:47:22Z有效 |

钱包两币种均15家列表来源、observedAt=null且retrieval_only；历史采用共同来源、无重复Gate别名，保留1–49小时的实际相邻间隔。名为3天的趋势在本次端点对应95/96小时，已披露实际时间；缺失来源被排除。ETF仍按08:30北京时间例行获取，最新已报告日09-23；09-24数据缺少基金分项，收盘后为unreconciled并退出累计，非交易日及未确认零值分别处理，已报告基金金额独立复算一致。钱包/溢价等辅助权重仍0，ETF独立展示。

1/4小时活动窗口覆盖100%；24小时BTC约57.5%、ETH约50.2%，缺少起始边界，正确降级。实验信号当前窗口已完整，但30天基线仍不足21有效日期/95%要求，保持观察中、没有信号；signalError、studyError、researchGap均为空。14天前向观察与90天历史研究未通过，SMTP未配置且未发送测试邮件。所有本次数据GET仅访问本地看板API，没有直接旁路调用CoinGlass。未将研究不足或SMTP待配置伪装成已验收，也未以这些独立项目替代本轮资源失败原因。

## V2.1 验证与连续运行（升级中止）

Go race/vet、前端类型/构建/基础测试通过。新增验证包括BTC/ETH和USDT/USD、多桶宽/范围金额对账、持续筛选不改变总额；大单重复/乱序/累计回退/补量/消失、历史饱和拆窗/空页/重启/429/认证错误、状态筛选冲突；90%有效分钟及首尾边界、CVD共源去重、OI原生粒度、HL统一8分钟有效期。在线备份恢复后新增小时摘要及容量缺口仍在。100次认证HTTP本地查询产生0次上游请求。

桌面/390px手机检查范围总额、交易所金额/报价/汇率/时间、重点/全部价位、活动页1/4/24小时和不足降级、大单历史分页与空结果、高级K线及存续线；原成交/合约/清算/巨鲸/健康路由回归。未发现页面横向溢出或控制台错误。发布脚本两种失败的隔离演练通过，不冒充新生产故障注入。实际接口state=3查询曾返回state=2，按原始事实保留并披露历史不完整。

v2.1.0已于13:50:35 UTC通过[正式Release](https://github.com/linlea666/tidal-crypto-intelligence/actions/runs/36008061214)自动部署；镜像构建后实际生产Compose、TLS、登录和受保护API检查通过，再由受限SSH入口切换，保留v2.0.0回退镜像。revision `cf4600120a0df94d9326fb695010346202c1448a`，镜像 `sha256:f649d17675dd9ca7dd103d090e5caedd0f84f76e8f1ff27698a8d0b0bbdaaae9`。

上线核对BTC/ETH各5/5现货有效，默认范围163/134档逐项来源贡献和总额一致。活动页1小时有效分钟100%，未接入逐笔成交的说明仍在。五分钟/八分钟巨鲸策略在目录、页面和监控共用；新版大单字段从新响应补存，不给旧记录编造初始数量。真实生产桌面/手机浏览器无横向溢出、无控制台错误。

V2.0在切换时中止，原起点保留：20个样本、1.598小时、健康失败0、峰值应用595.7MiB、最大间隔5.03分钟，不能标为72小时通过。完整报告与结束原因单独保存在本地忽略目录。

V2.1固定起点 **2026-09-24 13:50:41.815569 UTC**，满72小时为 **2026-09-27 13:50:41.815569 UTC**。文件分别为 `secrets/soak-v2.1-start.json`、`secrets/soak-v2.1-initial-report.json`、`secrets/soak-v2.1-latest.json`；不拼接V2.0样本。起点使用升级后首个基础数据可用的服务器采样；部分接口未提供来源时间，保持retrieval_only含义，历史窗口另行统计完整性。

前两个采样应用221.1/446.9MiB、CPU126.36%/128.23%（单核百分比），项目约698MB、SQLite3.51.3；容器重启0、OOM false，429为0，滚动额度不超过12，API短测约1.1–1.8秒（含网络/TLS）。这些是早期观测，CPU、常驻内存600MiB目标、历史增长、512MiB大单子预算和完整连续性仍待72小时验证。证书有效至2026-09-30 21:47:22 UTC。本段为上线初始记录；V2.1现已因V2.2正式切换中止，见上方结束证据，自动跟进已转至V2.2。

## 已验证

| 项目 | 证据与结果 |
| --- | --- |
| 计算与恢复 | Go race 测试涵盖汇率、合约单位、主动方向、去重、盘口序列、Kraken checksum、失效与恢复、空清算价、存储汇总和容量降级 |
| 构建 | 前端类型检查、生产构建、站点基础测试、Go vet 和脚本语法检查通过 |
| 真实部署 | GitHub stable Release 构建 Linux amd64 镜像；公开 GHCR 匿名拉取；镜像 OCI revision 与发布提交一致 |
| 生产容器 | Release 先在 CI 启动生产 Compose，验证 TLS、登录、受保护 API，再调用受限 SSH 部署入口 |
| 登录与权限 | 真实浏览器登录成功；未登录数据 API 返回 401；部署 SSH 密钥执行任意 `id` 命令被拒绝，退出码 64 |
| 真实行情 | 短时服务器观测 24/24 现货有效，BTC/ETH 三家合约数据有效；Hyperliquid 前50榜单、方向/均价/空清算价语义可见 |
| 证书 | 可信 IP HTTPS 正常；通过正式 Nginx 网关的 Certbot 模拟续期成功，关闭重复随机等待后耗时 9.1 秒 |
| 公开产物 | Git 跟踪文件中无 secrets、运行数据库、日志、私钥；本地凭据文件均被忽略 |

## 受控故障回退

首次演练目标为 v1.0.3，上一稳定版本为 v1.0.2。

- 07:41:38 UTC：仅停止匹配目标 revision 的新应用容器一次。
- 页面显示数据过期与服务暂时不可用；失效数据不伪装为仍在更新。
- 07:44:41 UTC：部署脚本自动恢复上一镜像和版本配置，`readyz` 正常。
- 演练前后抽查的持久历史记录 SHA-256 相同，持久化 `startedAt` 不变。
- 此次 GitHub 发布作业以失败结束是注入故障后的预期结果；之后通过新的正式版本恢复交付。

演练属于验收时的主动中断，不混入后续 72 小时正常运行统计。

## V1 连续验收记录（迁移中止）

V1的v1.0.4经 [正式 Release 工作流](https://github.com/linlea666/tidal-crypto-intelligence/actions/runs/35972704967) 部署成功。续期systemd服务实跑为success/0，续期与采样定时器均active。

验收从2026-09-24 08:05:55 UTC开始，按用户确认于12:02:36 UTC迁移至V2时中止，未达到72小时。47个正常样本没有就绪失败或OOM/重启，最大相邻间隔约5.04分钟，峰值应用346.4MiB；最后正常采样11:59:27 UTC，项目约582MB。这些是中止前短测结果，不能标为72小时通过。原起点保留，另存结束记录。原验收检查项：

- 容器重启、OOM、就绪错误和采样缺口。
- CPU、应用与网关内存趋势、磁盘和各类历史增长；应用常驻内存目标小于 900MB。
- 有效盘口、FX、合约及巨鲸新鲜度；区分可恢复断线和持续缺口。
- 查询延迟、请求失败、限流、细历史暂停与清理行为。
- 证书定时器、正式版本及前一稳定镜像保留。

短时资源样本和本地回放基准不能替代以上连续运行结果。验收起点记录在本地已忽略的 `secrets/soak-start.json`，详细服务器报告覆盖写入 `secrets/soak-latest.json`；公开文档只记录汇总结论。

起点后的首个样本为 08:08:29 UTC：24/24 现货有效；BTC 53/53、ETH 55/55 个已观察核心持仓新鲜；应用 139.8 MiB、网关 3.8 MiB，应用 CPU 为单核口径约 60%；项目总占用约 479MB。无 OOM / 容器重启；服务器健康与就绪两次查询合计约 4.4ms；本地客户端完整 HTTPS API 请求约 0.96–1.31 秒（包括网络与 TLS）。这只是首个正常运行样本。

## V2 新增验收与切换

用户确认V2就绪后直接切换，V1不等待满72小时。原 `secrets/soak-start.json` 保留；切换原因和时间单独记录，结论为中止/未完成。

v2.0.0已于12:02:36 UTC切换，正式Release的race、镜像、生产Compose/TLS/认证及自动部署均成功：[工作流证据](https://github.com/linlea666/tidal-crypto-intelligence/actions/runs/35996240181)。生产实际SQLite为3.51.3，旧采集器不再运行，保留v1.0.4镜像可回退。

在线备份采用实际运行引擎：本地备份恢复后100次认证HTTP查询的上游增量为0；未认证查询返回401。生产V2运行中以只读源挂载执行在线备份，备份完整性检查ok。对当前部署脚本的隔离演练分别注入Compose启动失败和就绪检查失败，验证恢复上一镜像/配置且不修改成功部署版本。该脚本演练不冒充新的生产故障注入；真实跨版本生产回退证据为上文V1演练。

已通过本地测试：100次本地查询零上游请求、并发同一按需请求只创建一个任务、额度滚动窗口与重启账本、429冷却/认证暂停、重复/修正/乱序、细汇总不覆盖完整粗历史、按数据集保留/删除保护、成交求和及VWAP输入、存量末值、模型最新切片、空足迹/空清算价、闭合周期、FX失效、观察间隙、SQLite在线备份还原。真实响应契约在本地忽略目录验证。

V2需在正式部署后单独完整运行72小时。每5分钟服务器样本应含 generation=v2、源时间/获取时间、有效盘口、FX/价格、逐条巨鲸新鲜度、调度额度/错误、内存/CPU/磁盘。受控发布和故障演练区间不算正常运行。开始文件使用 `secrets/soak-v2-start.json`，完整采样为 `secrets/soak-v2-latest.json`。

验收必须检查：连续性与版本固定、无OOM/意外重启、常驻内存目标600MiB、容器上限768MiB、滚动12次限额、无持续关键源失效、磁盘20GB/巨鲸2GB/空闲8GB、清理与历史增长、API延迟和证书剩余时间。回看数据可能明确显示不完整，不得以空白/旧缓存假装实时有效。完整72小时结果目前尚未得出。

## V2 本轮起点与首个正常样本

- 固定起点：2026-09-24 12:14:41.186604 UTC；满72小时为2026-09-27 12:14:41.186604 UTC。12:02:36切换至起点之间为启动预热，单独排除。
- 版本v2.0.0，revision `73744f29ca506d07561b140456c9d82fc33cc53b`，镜像 `sha256:945119ca5b97295beb5bc8ff0f5c5fa85fd48af647cee9033ba3a2b1fc1fc6cb`。不能将文档提交的main最新SHA误当作生产版本变化。
- 核心基础数据均为有效或已获取/来源时间未知；BTC/ETH各5/5盘口，FX与即时价格有效。巨鲸符合180秒要求的持仓为BTC 276/322、ETH 202/238，过期行退出实时汇总；不要求上游所有地址同时更新，也不把失效行算入金额。
- 应用66MiB、单核口径CPU5.71%；网关2.844MiB；项目594,083,094字节，空闲34,784,677,888字节。无OOM/意外重启、无历史暂停。SQLite3.51.3。
- 累计138次代理请求，当前滚动窗口12次，429为0、未认证失败。资金费率、足迹、OI和清算模型已完成首次采集；历史补齐继续进行。
- 线上浏览器确认币种/价格对应、前100榜单与临近清算排序、模型高级图、手机页面不横向溢出；错误日志为空。HTTPS认证业务查询短测约1.0–1.5秒，包含网络与TLS，不能等同服务内执行时间。

初始完整报告保存在已忽略的 `secrets/soak-v2-initial-report.json`。每小时自动跟进已启用，正常无变化时保持安静。派生分钟持仓摘要会继承参与仓位中最早的来源更新时间，可能早于下次计算到期；应结合实时逐条有效仓位和基础源判断，不能单凭旧摘要状态认定持续断流。历史补齐与来源时间未知也单独核对，不视为已经完整。

本节记录的是验收起点，**不是完整72小时验收通过**。到期后必须核对全部采样、版本、资源和数据质量再写最终结论；采样不完整或发生持续异常则如实记录并停止本轮，不静默延长。
