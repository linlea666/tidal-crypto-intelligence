# 实施状态

## V2.3 发布前验证

独立大额挂单、三块成交解读、BTC专用预警/研究与小时案例补采已经实现。137条固定快照分页、小时降级不混入五分钟策略、分范围失败及恢复、通知范围、GET零上游与原算法等价有自动测试。Go race与前端类型/构建通过；在线备份恢复与部署失败隔离回退通过。

受限Linux资源回放已通过：1.7核、768MiB容器，90天补采/完整研究/并发本地读，280秒、62个样本，峰值40.66MiB、末值39.06MiB，无OOM。该短测是发布门槛，不能当72小时资源通过。最终提交将重新运行CI检查。真实小时数据四组各840条，三个指定案例可读；五分钟完整策略检验仍可能不足。

V2.2.1资源失败结论及原起点/结束保持不变。V2.3尚未切换生产、尚未建立验收起点。详情见 [V2.3数据与页面说明](V2.3-ORDERS-STUDIES.md)。


## 当前：V2.2 已部署，本轮资源验收未通过

2026-09-25北京时间，[v2.2.1](https://github.com/linlea666/tidal-crypto-intelligence/releases/tag/v2.2.1) 已经本地→GitHub检查→正式Release→公开GHCR摘要→服务器部署。主功能经 [PR #3](https://github.com/linlea666/tidal-crypto-intelligence/pull/3) 合并，最终revision为 `89461c165b5be038d60f3d06795e021a8460caf5`，镜像为 `sha256:e54614c8d264e14c388462bdc2af81e50aece0ba19b909431cb7677177d834c9`。V2.2.0首轮真实核验后，V2.2.1补齐钱包未加载或全空时的API空值语义；有效零余额仍保留为0，正常挂单和余额金额算法不变。

已交付：大资金动向中的实验性异动/价格确认、历史复盘与钱包趋势；独立BTC/ETH ETF资金页；事实修订/首次可用时间、去重研究队列、持久信号状态及可选SMTP通知。CVD不重复计分，钱包、OI、溢价等辅助指标权重为0，ETF不参与日内信号；不新增财经日历、新闻或ERC-20采集。详见 [V2.2规则和数据说明](V2.2-SIGNALS-WALLET-ETF.md)。

新增9个数据集已由唯一生产调度器通过真实返回契约检查：BTC/ETH钱包列表和历史、BTC/ETH ETF、BTC/ETH聚合OI历史、BTC Coinbase溢价。两个钱包列表各15家有效来源，历史合并Gate别名并按共同覆盖计算，实际间隔/缺失来源可见。ETF最新已报告日为2026-09-23，09-24未结束记录不作为真实零值；部分旧日期基金字段为空时从累计中排除。溢价暂只显示美元差，原始比例单位未独立验证。

已通过Go财务/存储/恢复/race、vet、前端类型/构建/基础测试、在线备份恢复、隔离启动失败/健康失败回退。暂停采集后的100次本地读取为零上游调用，重复研究请求合并。桌面1440px和手机390px复核钱包/ETF真实数据、基金逐日切换、BTC/ETH、预警不足降级、研究任务与已有页面，无页面横向溢出或浏览器错误。真实生产部署的Compose/TLS/登录健康检查通过。

V2.1原起点保留，在 **2026-09-24T17:38:44.586135573Z** 切换时记为中止：3.801小时、46个正常版本样本，健康失败0，峰值应用635.8MiB，最大间隔约5.15分钟。曾有一次本地HTTPS握手超时，同期服务器就绪采样正常；不把短测或峰值当作72小时通过。原始结束及完整报告均保存在已忽略目录。

V2.2固定起点为 **2026-09-24T17:51:43.474629Z**，原计划满72小时为 **2026-09-27T17:51:43.474629Z**，以最终稳定版本预热后的首个合格服务器采样为准。文件为 `secrets/soak-v2.2-start.json`、`soak-v2.2-initial-report.json`、`soak-v2.2-latest.json`；V2.2.0启动/契约核验及补丁切换区间独立保存，不拼接计时。本轮现已因持续未达到内存目标提前结束；原起点不变，结束及完整报告另存 `soak-v2.2-end.json`、`soak-v2.2-final-report.json`。

2026-09-25北京时间08时复核截至 **2026-09-25T00:03:01.333900Z** 的75个采样，共6.188小时，最大间隔301.97秒。版本/revision/digest及容器镜像固定，没有健康/就绪失败、重启或OOM。容器内存（Docker MemUsage口径）46/75个采样超过600MiB，最近4小时33/48个超过目标、中位数631.85MiB，峰值711.1MiB，末值648.9MiB。持续资源指标未达到约定目标，因此记为**未通过，提前结束**，停用本轮自动验收；线上应用和服务器采样继续运行。没有重置起点、发布新版本或拼接72小时。内存分配根因尚需独立诊断，当前证据不足以判定内存泄漏。

同期BTC/ETH始终各5家盘口有效、价格/FX有效，默认清算模型、大单及逐条巨鲸采集继续；起点后新增429为0，采样中的滚动60秒最多12次、并发最多1个。项目约891.8MiB、研究约33.9MiB、磁盘空闲31.9GiB，容量保护未触发。ETH聚合OI历史在10个采样中短暂过期，已获取的有效时点多次约10分钟才推进，源时间加12分钟的有效期正确降级；BTC溢价另有2个过期采样。两个7天清算按需缓存过期属于已完成请求，不计作默认实时模型断流。详细证据见 [ACCEPTANCE](ACCEPTANCE.md)。

当前generation=v2、旧采集器关闭、BTC/ETH各5/5盘口、价格/FX有效，SQLite3.51.3。初始后续观测项目约737.4MiB、研究库约5.7MiB、磁盘空闲约32.0GiB；当前数据采集、研究写入未暂停。429累计值1来自V2.1旁路探测，已恢复，不能误记为V2.2新增错误；起点记录保存计数基准。

**尚未完成**：完整72小时资源/额度/增长验收（本轮资源项未通过）、90天历史研究及14天前向观察。基线或当前窗口不足会停发实验预警，不显示预测胜率，不放宽质量门槛。SMTP配置尚未提供，站内功能可用，邮件实发未验收。需要配置的字段和路径见V2.2说明。

研究队列后续实取确认：BTC/ETH现货和合约的4个历史任务首批5分钟窗口未返回有效数据，2个OI历史及1个溢价历史任务返回业务码400；各自5次失败后按上限停用。价格游标已到请求终点，研究结果已标为 `incomplete`，未计算核心策略成绩。这不能证明整个90天区间均不可用；须先核实历史请求契约和可取范围。最新9个新增数据集仍为verified，辅助指标权重为0。1/4小时活动窗口完整；24小时BTC/ETH覆盖约57.5%/50.2%且缺少起点，正确显示证据不足。实验预警因30天基线不足保持观察中，未触发信号或邮件。

## V2.1 历史记录（升级中止）

2026-09-24 13:50:35 UTC（北京时间21:50:35），`v2.1.0` 已通过 [正式Release工作流](https://github.com/linlea666/tidal-crypto-intelligence/actions/runs/36008061214) 部署。[PR #2](https://github.com/linlea666/tidal-crypto-intelligence/pull/2) 及主分支检查通过；生产revision为 `cf4600120a0df94d9326fb695010346202c1448a`，镜像为 `sha256:f649d17675dd9ca7dd103d090e5caedd0f84f76e8f1ff27698a8d0b0bbdaaae9`，上一稳定v2.0.0保留可回退。

已完成挂单范围总额/单档口径、来源时间与覆盖说明，以及独立“大资金动向”页面。新增共享大单生命周期、结束/撤销窗口补采、原始状态与累计值修正处理；大单5分钟采集/12分钟有效，Hyperliquid5分钟/逐条8分钟有效，常规约10.63请求/分钟。

本地已通过Go财务/恢复/存储及race、vet、前端类型/构建/4项基础测试、认证HTTP100次本地查询零上游、在线备份恢复新增表、发布脚本启动/健康失败的隔离回退。桌面及390px手机交互确认总额、单档、来源明细、1/4/24小时窗口、空缺降级、分页及高级视图；控制台无错误。

真实代理补测及上线后均发现state=3筛选返回rawState=2的记录：保留“已结束”，不认定撤销，持久标记对应历史覆盖不完整；相关回归测试通过。逐笔大额成交仍未接入。详见 [V2.1数据契约](V2.1-DATA-ACTIVITY.md)。

生产核对BTC/ETH各5/5盘口，默认范围分别163/134档，交易所贡献之和等于本档金额、全部价位之和等于范围总额；1小时活动窗口覆盖100%，接口返回同窗成交及价格反馈。刷新配置实际为大单300/720秒、Hyperliquid300/480秒。成交、合约、清算、巨鲸接口回归正常，旧采集器仍关闭。真实桌面/390px手机无横向溢出、浏览器错误为空。新字段从首轮新版采集开始补存，历史补齐范围及代理状态差异均披露。

V2.0原起点12:14:41 UTC保持不变，在13:50:35切换时中止，累计1.598小时、20个正常版本样本，未发现健康失败；峰值应用595.7MiB，最大相邻间隔5.03分钟。这是中止前短测，不是72小时通过，结束记录为已忽略的 `secrets/soak-v2-end.json`。

V2.1独立固定起点为2026-09-24 **13:50:41.815569 UTC**，采用升级后首个当前基础数据可用的服务器采样；满72小时为2026-09-27同一时刻（北京时间21:50:41）。原有有效缓存恢复后继续按源时间/获取时间判断，不伪造新数据；缺失历史另列。原起点保存在 `secrets/soak-v2.1-start.json`；切换时完整报告另存 `secrets/soak-v2.1-final-report.json`，结束记录为 `secrets/soak-v2.1-end.json`。本轮已中止，不再继续旧计时。

前两个样本应用221.1/446.9MiB，单核口径CPU126.36%/128.23%（2核机器，不能当作稳态结论）；项目约698MB，SQLite3.51.3。没有OOM/重启、429或认证错误；公开HTTPS查询短测约1.1–1.8秒，含网络及TLS。当前未满72小时，不认定长期资源与增长验收通过。

## V2.0 历史记录（升级中止）

2026-09-24 12:02:36 UTC（北京时间20:02:36），`v2.0.0` 已通过 [正式 Release 工作流](https://github.com/linlea666/tidal-crypto-intelligence/actions/runs/35996240181) 部署。提交 `73744f29ca506d07561b140456c9d82fc33cc53b`，不可变镜像 `sha256:945119ca5b97295beb5bc8ff0f5c5fa85fd48af647cee9033ba3a2b1fc1fc6cb`；上一稳定版本保留为v1.0.4。代码经 [PR #1](https://github.com/linlea666/tidal-crypto-intelligence/pull/1) 合并，生产机没有本地构建。

CoinGlass共享数据层已接管；仅保留币安价格/K线和Kraken美元汇率直连。线上确认 `legacyCollectorsRunning=false`，BTC/ETH各5家现货有效，SQLite实际3.51.3。关闭本地采集后再启用生产采集，避免共享代理额度竞争。当前基础数据首轮采集已完成，后台继续补大额挂单历史和30天基线；样本不足/来源时间未知仍明确显示。

验证包括Go race及财务/恢复/存储、前端类型/构建、桌面/手机交互、GitHub生产Compose/TLS/认证检查。真实在线备份恢复后的100次本地HTTP查询新增上游请求为0；未登录请求返回401。生产V2数据库再次执行在线备份，完整性检查ok。启动失败和健康检查失败的隔离脚本演练均恢复原镜像/配置；V2未另外向生产注入故障，历史生产回退证据见下文。两个运维定时器active，最近续期服务success/0。

V1验收已按用户确认在切换时中止：原起点08:05:55 UTC不改，累计不足4小时，47个正常样本，未发现就绪失败；不能认定72小时通过。结束记录单独保存为已忽略的 `secrets/soak-v1-end.json`。V2使用独立起点与验收证据，完整72小时结论尚未得出。

V2正式验收起点：2026-09-24 12:14:41 UTC（北京时间20:14:41），采用预热后首个当前基础数据就绪的服务器采样；满72小时为2026-09-27同一时刻。起点固定保存在 `secrets/soak-v2-start.json`，初始完整报告单独存档。服务器每5分钟采样，已启用每小时跟进，正常无变化时不打扰；结束时按证据记录通过或不完整/未通过，不自动重置。

起点样本：应用66MiB、CPU约单核5.71%，网关2.84MiB；项目约594MB，磁盘空闲约34.8GB。BTC/ETH各5/5盘口、FX和价格有效；已覆盖巨鲸持仓中276/322与202/238条符合逐条180秒要求，过期仓位退出实时金额。累计138次代理调用，429为0，滚动窗口12次。无OOM/重启，历史写入未暂停。这是一个短时采样，不代表72小时资源结论。

线上浏览器复核：BTC/ETH、现货柱/详情、巨鲸前100和临近清算排序、清算高级图；手机390px视口无页面横向溢出，控制台错误为空。原有登录在迁移后仍可使用。

使用见 [USAGE](USAGE.md)，数据字典、复用关系、刷新/过期与新功能接入见 [V2-DATA-LAYER](V2-DATA-LAYER.md)。

## V1 历史记录（已结束）

2026-09-24，V1 首次实现。

已完成：
- 选定柱状界面、六个页面、认证和手工价格标注。
- 24 个现货盘口、本地真实连接均有效；合约三家 OI / Funding / 基差；Hyperliquid 公开持仓与空清算价处理。
- Go 数值 / 状态 / 存储 / 并发测试及 race 检查；前端类型和构建；Docker / Release 发布脚本。
- 24 个各 5,000 档双边盘口回放聚合：本地 Apple M3 约 46 ms / 次，约 11.5MB 临时分配 / 次。该基准不代表目标服务器表现。

已完成：桌面 / 手机布局与主要浏览器流程验收（见 `design-qa.md`）；首轮 GitHub CI；服务器受限 SSH 部署入口与监控入口；IP HTTPS 证书签发及续期定时器配置。

已通过：v1.0.0 的 GitHub 构建和公开 GHCR 匿名拉取；服务器 Certbot IP 证书续期 dry-run。

首次部署发现：Compose 行内数组把 tmpfs 的逗号选项拆为两个挂载，应用容器尚未启动。v1.0.1 修正引号，并加入实际 Compose + TLS + 登录的发布前容器检查。服务器运维采样补充盘口覆盖、FX 时间、合约和巨鲸新鲜度，便于 72 小时验收。

v1.0.1 容器启动、CI HTTPS / 登录检查通过，服务器应用 healthy。公网 8443 未开放，生产检查未通过；已确认云防火墙现有 443 规则。v1.0.2 使用现有标准 HTTPS 入口，2026-09-24 07:26 UTC 自动部署成功，浏览器登录、实际行情和未登录 API 返回 401 已验证。

服务器短时采样：24/24 现货有效；应用约 130–138 MiB，CPU 约单核 44–58%；Nginx 约 2 MiB。公开 HTTPS 的本地客户端完整请求约 1–1.4 秒（含网络与 TLS）；服务器健康请求约 13ms。不是72小时验收结果。

v1.0.3 补充使用说明与监控失败报告；默认选中真实数据中的价位，并在重点列表保留用户已选行。币种切换、展开/收起后选中行保留、控制台无错误已在浏览器验证。

跨版本回退演练已通过：2026-09-24 07:41:38 UTC 仅停止 v1.0.3 新应用容器，07:44:41 UTC 自动恢复 v1.0.2，`readyz` 正常；历史记录的 SHA-256 与持久化采集起点均未改变。v1.0.3 首次发布任务因此按预期标记失败，不代表回退失败。

续期复测发现 Certbot 自带随机等待可超过 systemd 的 180 秒超时。v1.0.4 保留定时器错峰，关闭 Certbot 内部的第二次随机等待；通过实际生产网关的续期 dry-run 已成功，耗时 9.1 秒。v1.0.4 已经 GitHub Release 自动部署成功；实际续期服务执行结果 success / 0，两个运维定时器 active，上一稳定镜像 v1.0.2 保留。

切换前线上版本：`v1.0.4`，提交 `451658f2818e5dc9cf9360fe262608ee72397123`，镜像摘要 `sha256:2190b197deab2125b1b8639558bebd1fc737efe7859b4cb6f84ca6a2f60c701f`。真实桌面 / 手机截图和币种切换、有效默认选择已复核；浏览器错误记录为空。

已中止：V1连续72小时资源与数据增长验收，从2026-09-24 08:05:55 UTC开始，在12:02:36 UTC迁移切换时中止。故障演练已排除；短时测试不视为72小时完成。详细证据与标准见 [ACCEPTANCE](ACCEPTANCE.md)。

## V2 改造（2026-09-24）

已在 `codex/tidal-v2-data-layer` 实现共享数据层、统一调度/额度、本地只读查询、五家现货、CoinGlass成交/合约/清算/巨鲸、历史等级及前50/100分析。即时行情仅保留币安，美元汇率保留Kraken；V2启动时旧多所盘口、合约、Hyperliquid扫描/订阅均不再启动。详细契约与边界见 [V2-DATA-LAYER](V2-DATA-LAYER.md)。

本地真实接口短测：BTC/ETH各5/5盘口有效，已观察300余次受控代理调用未遇429；资金费率中缺失字段的交易所行被跳过，不填0。钱包详情实际遇到过一次业务500，另一个公开地址成功，前端按真实状态显示。短测不是完整72小时验收。

本地通过：Go财务/恢复/存储测试与race、前端类型/构建/现有站点测试；桌面及手机视口、BTC/ETH切换、成交、巨鲸前100和距清算排序、清算柱/高级图交互检查。SQLite实际版本3.51.3；在线备份恢复WAL测试通过。

用户已确认：新版通过发布检查后直接切换；旧v1.0.4验收在切换时记为迁移中止，原起点不改。V2发布已执行；不将本地短测计为完整连续验收通过。V1最后47个正常样本无就绪失败、无OOM/重启，峰值应用346.4MiB，仍不足72小时。
