# 实施状态

2026-09-24，V1 首次实现。

已完成：
- 选定柱状界面、六个页面、认证和手工价格标注。
- 24 个现货盘口、本地真实连接均有效；合约三家 OI / Funding / 基差；Hyperliquid 公开持仓与空清算价处理。
- Go 数值 / 状态 / 存储 / 并发测试及 race 检查；前端类型和构建；Docker / Release 发布脚本。
- 24 个各 5,000 档双边盘口回放聚合：本地 Apple M3 约 46 ms / 次，约 11.5MB 临时分配 / 次。该基准不代表目标服务器表现。

已完成：桌面 / 手机布局与主要浏览器流程验收（见 `design-qa.md`）；首轮 GitHub CI；服务器受限 SSH 部署入口与监控入口；IP HTTPS 证书签发及续期定时器配置。

已通过：v1.0.0 的 GitHub 构建和公开 GHCR 匿名拉取；服务器 Certbot IP 证书续期 dry-run。

首次部署发现：Compose 行内数组把 tmpfs 的逗号选项拆为两个挂载，应用容器尚未启动。v1.0.1 修正引号，并加入实际 Compose + TLS + 登录的发布前容器检查。服务器运维采样补充盘口覆盖、FX 时间、合约和巨鲸新鲜度，便于 72 小时验收。

v1.0.1 容器启动、CI HTTPS / 登录检查通过，服务器应用 healthy。公网 8443 未开放，生产检查未通过；已确认云防火墙现有 443 规则。v1.0.2 使用现有标准 HTTPS 入口，2026-09-24 07:26 UTC 自动部署成功，浏览器登录、实际行情和未登录 API 返回 401 已验证。

服务器短时采样：24/24 现货有效；应用约 130–138 MiB，CPU 约单核 44–58%；Nginx 约 2 MiB。公开 HTTPS 的本地客户端完整请求约 1–1.4 秒（含网络与 TLS）；服务器健康请求约 13ms。不是72小时验收结果。

v1.0.3 补充使用说明与监控失败报告；默认选中真实数据中的价位，并在重点列表保留用户已选行。币种切换、展开/收起后选中行保留、控制台无错误已在浏览器验证。

跨版本回退演练已通过：2026-09-24 07:41:38 UTC 仅停止 v1.0.3 新应用容器，07:44:41 UTC 自动恢复 v1.0.2，`readyz` 正常；历史记录的 SHA-256 与持久化采集起点均未改变。v1.0.3 首次发布任务因此按预期标记失败，不代表回退失败。

续期复测发现 Certbot 自带随机等待可超过 systemd 的 180 秒超时。v1.0.4 保留定时器错峰，关闭 Certbot 内部的第二次随机等待；通过实际生产网关的续期 dry-run 已成功，耗时 9.1 秒，修正正在按正式版本发布。

待开始：完整系统连续 72 小时资源与数据增长验收。完成故障演练和最终部署后单独计时，短时接口测试不视为已完成。
